{
  "name": "mex-mapa",
  "version": "1.0.0",
  "description": "Sistema operativo de patio para gestión de flota vehicular",
  "private": true,
  "scripts": {
    "build:papeletas": "tsc -p tsconfig.papeletas.json && node scripts/copy-ts-output.js",
    "watch:papeletas": "tsc -p tsconfig.papeletas.json --watch",
    "bump-sw": "node scripts/bump-sw.js",
    "deploy": "npm run build:papeletas && node scripts/bump-sw.js && firebase deploy --only hosting --project production",
    "deploy:full": "npm run build:papeletas && node scripts/bump-sw.js && firebase deploy --project production",
    "deploy:staging": "npm run build:papeletas && node scripts/bump-sw.js && firebase deploy --only hosting --project staging",
    "deploy:staging:full": "npm run build:papeletas && node scripts/bump-sw.js && firebase deploy --project staging",
    "deploy:functions": "npm run security:preflight && firebase deploy --only functions --project production",
    "deploy:rules": "npm run test:security:rules && firebase deploy --only firestore:rules,storage --project production",
    "security:audit:credentials": "node scripts/purge-plaintext-passwords.js",
    "security:preflight": "node scripts/security-preflight.js",
    "test:security:hosting": "firebase emulators:exec --only hosting \"node scripts/test-hosting-surface.js\"",
    "test:security:rules": "firebase emulators:exec --only firestore \"node scripts/test-security-baseline-rules.js\"",
    "test:security": "npm run security:preflight && npm run test:security:rules",
    "test:messages:identity": "node --test domain/mensajes-identity.model.test.mjs",
    "test:pdf-export": "firebase emulators:exec --only functions,firestore,auth \"node scripts/test-generar-pdf.js\""
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.2.4",
    "cloudinary": "^2.10.0",
    "tailwindcss": "^4.2.4"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "playwright": "^1.60.0",
    "typescript": "^7.0.2"
  }
}
